extends layout

block append vars
  - bodyclass = 'article-detail'

block prepend title
  | #{ page.title + " - "}

block header
  h1= page.title


block content
  article.article
    section.content!= typogr(page.html).chain().widont().smartypants().value().replace(/(<div id="TOC">[\s\S]*<\/div>)([\s\S]+?)(<h2)/, "$2<h2>Table of Contents</h2>$1$3")

block prepend footer
  div.nav
    a(href=contents.topics.index.url) « Topics List


