extends layout

block append vars
  - bodyclass = 'page'

block prepend title
  | #{ page.title + " - "}

block header
  h1= page.title

block content
  section.content!= typogr(page.html).chain().amp().widont().smartypants().caps().initQuotes().value()
